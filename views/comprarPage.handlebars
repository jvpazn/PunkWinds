<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="icon" href="/img/favicon.png" />
    <title>Finalizar Compra - {{game.gameName}}</title>
    <link href="https://fonts.googleapis.com/css2?family=Sedgwick+Ave+Display&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/gamePage.css"> 
</head>
<body>
    <main id="Content" class="main-content">
        <h1>Checkout</h1>
        
        <div class="checkout-summary">
            <img src="/img/{{game.gameImg}}.png" class="game-img">
            <h2>{{game.gameName}}</h2>
            <p class="price-total">Total: R$ {{game.gamePrice}}</p>
        </div>

        <div class="payment-container">
            <h3>Escolha seu método de pagamento:</h3>

            {{#if temMetodos}}
                <form action="/comprar/finalizar" method="POST" class="payment-form">
                    <input type="hidden" name="id" value="{{id}}">
                    <input type="hidden" name="idgames" value="{{game.id}}">

                    <div class="method-list">
                        {{#each metodos}}
                        <label class="method-label">
                            <input type="radio" name="idPagamento" value="{{this.id}}" required class="method-radio">
                            <span>
                                <strong>{{this.apelido}}</strong> 
                                <small>({{this.tipo}} - {{this.detalhes}})</small>
                            </span>
                            
                            <button form="form-remove-{{this.id}}" class="remove-button">X</button>
                        </label>
                        {{/each}}
                    </div>

                    <button type="submit" id="buyButton">Confirmar Pagamento</button>
                </form>

                {{#each metodos}}
                <form id="form-remove-{{this.id}}" action="/user/pagamento/remover" method="POST" class="remove-form">
                    <input type="hidden" name="id" value="{{../id}}">
                    <input type="hidden" name="idPagamento" value="{{this.id}}">
                    <input type="hidden" name="idgames" value="{{../game.id}}">
                    <input type="hidden" name="origem" value="comprar">
                </form>
                {{/each}}

            {{else}}
                <p>Você não possui métodos de pagamento salvos.</p>
            {{/if}}

            <hr class="separator">

            <h3>Adicionar Novo Cartão/Método</h3>
            <form action="/user/pagamento/adicionar" method="POST" class="add-method-form">
                <input type="hidden" name="id" value="{{id}}">
                <input type="hidden" name="origem" value="comprar">
                <input type="hidden" name="idgames" value="{{game.id}}">

                <div>
                    <label>Tipo:</label>
                    <select name="tipo" required>
                        <option value="Cartão de Crédito">Cartão de Crédito</option>
                        <option value="Cartão de Débito">Cartão de Débito</option>
                        <option value="Pix">Pix</option>
                        <option value="PayPal">PayPal</option>
                    </select>
                </div>

                <div>
                    <label>Apelido (ex: Nubank Principal):</label>
                    <input type="text" name="apelido" required placeholder="Apelido para identificar">
                </div>

                <div>
                    <label>Detalhes (ex: Final 4321 ou Chave Pix):</label>
                    <input type="text" name="detalhes" required placeholder="**** **** **** 1234">
                </div>

                <button type="submit" id="botao-maior">Salvar e Usar</button>
            </form>
        </div>

        <br>
        <a href="/game?idgames={{game.id}}&id={{id}}">
            <button id="botao-maior">Cancelar</button>
        </a>
    </main>
</body>
</html>